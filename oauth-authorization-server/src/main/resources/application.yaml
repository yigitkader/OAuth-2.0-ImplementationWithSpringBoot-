server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user-registration
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update